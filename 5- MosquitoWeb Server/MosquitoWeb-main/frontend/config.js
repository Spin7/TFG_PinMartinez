/**
 * config.js — MosquitoAI Frontend Configuration
 *
 * Set BACKEND_URL to the URL of your deployed Railway backend.
 * Leave empty string "" to use the same origin (useful when testing locally
 * with the FastAPI server also serving the static files).
 *
 * Examples:
 *   window.BACKEND_URL = "https://mosquitoai-backend.up.railway.app";
 *   window.BACKEND_URL = "http://localhost:8000";   // local dev
 */
window.BACKEND_URL = "https://web-production-90e52.up.railway.app";   // ← Replace with your Railway URL before deploying
